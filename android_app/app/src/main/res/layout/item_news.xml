<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?selectableItemBackground"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:padding="16dp"
    tools:ignore="Overdraw">

    <ImageView
        android:id="@+id/news_image"
        android:layout_width="96dp"
        android:layout_height="96dp"
        android:layout_marginEnd="16dp"
        android:contentDescription="@string/content_description_image"
        android:scaleType="centerCrop"
        tools:src="@drawable/icon_big" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            android:id="@+id/news_title"
            style="@style/font_roboto_regular"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="2"
            android:textColor="@color/text_color"
            android:textSize="16sp"
            tools:text="Boruto - Naruto Next Generations - 09-11" />

        <TextView
            android:id="@+id/news_text"
            style="@style/font_roboto_light"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:ellipsize="end"
            android:maxLines="2"
            android:textColor="@color/text_color_black_grey"
            android:textSize="13sp"
            tools:text="Lorem fistrum qué dise usteer está la cosa muy malar pecador torpedo." />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="2dp"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/news_fansub"
                style="@style/font_roboto_regular"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:maxWidth="136dp"
                android:textColor="@color/text_color"
                android:textSize="12sp"
                tools:text="Ramen Amb Pa amb Tomàquet" />

            <TextView
                android:id="@+id/news_date"
                style="@style/font_roboto_light"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:maxLines="1"
                android:textColor="@color/text_color_white_grey"
                android:textSize="12sp"
                tools:text="04/02/2018 10:18" />
        </LinearLayout>
    </LinearLayout>
</LinearLayout>
